# bitstream/
 
`FPGA_bitstream_MCU.bin` — pre-built bitstream for Shrike-lite.
 
Flash to board via Thonny (rename to `serv_shrike.bin`), then run `firmware/main.py`.
 
Generated by: Go Configure Software Hub from `ffpga/` sources.
Go Configure may name this file `FPGA_bitstream_FLASH_MEM.bin` depending on version.
The correct file is always the ~45 KB `.bin` in `ffpga/build/bitstream/`.
